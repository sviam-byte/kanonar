
export enum EntityType {
  Character = 'character',
  Object = 'object',
  Concept = 'concept',
  Place = 'place',
  Location = 'location',
  Protocol = 'protocol',
  Event = 'event',
  Document = 'document',
  Essence = 'essence',
  SocialEvent = 'social_event',
}

export enum Branch {
  Current = 'current',
  PreRector = 'pre-rector',
  PreBorders = 'pre-borders',
}
